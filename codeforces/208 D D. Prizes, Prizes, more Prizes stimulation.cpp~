#include <iostream>
#include <cstdio>
#include <string.h>
#include <math.h>
using namespace std;
int main() {
    double n,m,i,g,a;
    int long long k;
    while (scanf("%lf %lf %lf",&n, &m, &a)==3) {
        //printf("%lf\n",a);
        g=ceil(n/a);
        //printf("%lf %lf %lf\n",g,m,a);
        if(m>a) {
            //printf("float akta baalchal\n");
            m-=a;
            //printf("%lf\n",ceil(m/a));
            k=g+g*(ceil(m/a));
        }
        else k=g;
        cout<<k<<endl;
    }
    return 0;

}
