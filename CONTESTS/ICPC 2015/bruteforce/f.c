#include <stdio.h>
#include <cstring>
#include <climits>
int main()
{
    int t,i,k=0,flag;
    char str[100100];
    scanf("%d",&t);
    while(k<t)
    {
        scanf("%s",str);
        int len= strlen(str);
        for(int i=0;i<len;i++)
        {
            if(str[i]== '^')
            {

            }
        }
        k++;
    }
    return 0;
}

